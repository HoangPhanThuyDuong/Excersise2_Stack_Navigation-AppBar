import { Button, Text, View } from "react-native"

const DemoProfileScreen =({navigation, route})=>{
    return(
        <View style={{flex:1, justifyContent:"center", alignItems:"center"}}>
            <Text> Profile Screen </Text>
        </View>
    )
}
export default DemoProfileScreen